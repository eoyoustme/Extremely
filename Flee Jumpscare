		local Players = game:GetService("Players")
		local TweenService = game:GetService("TweenService")

		local player = Players.LocalPlayer
		local playerGui = player:WaitForChild("PlayerGui")
		local rng = Random.new()

		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "JumpscareGui"
		screenGui.ResetOnSpawn = false
		screenGui.IgnoreGuiInset = true
		screenGui.DisplayOrder = 999999
		screenGui.Parent = playerGui

		-- Tạo background
		local background = Instance.new("Frame")
		background.Name = "Background"
		background.Size = UDim2.fromScale(1, 1)
		background.Position = UDim2.fromScale(0, 0)
		background.BackgroundColor3 = Color3.new(0, 0, 0)
		background.BorderSizePixel = 0
		background.Parent = screenGui
		background.ZIndex = 1
		
		local background2 = Instance.new("Frame")
		background2.Name = "Background2"
		background2.Size = UDim2.fromScale(1, 1)
		background2.Position = UDim2.fromScale(0, 0)
		background2.BackgroundColor3 = Color3.new(0, 0, 0)
		background2.BorderSizePixel = 0
		background2.Parent = screenGui
		background2.Transparency = 1
		background2.ZIndex = 2

		-- Tạo ImageLabel
		local imageLabel = Instance.new("ImageLabel")
		imageLabel.Name = "ImageLabel"
		imageLabel.BackgroundTransparency = 1
		imageLabel.AnchorPoint = Vector2.new(0.5, 0.5)
		imageLabel.Position = UDim2.fromScale(0.5, 0.5)
		imageLabel.Size = UDim2.new(0, 150, 0, 150)
		imageLabel.Image = "rbxassetid://14167462590"
		imageLabel.Parent = background
		imageLabel.ZIndex = 10
		
		local tw = TweenService:Create(
			background,
			TweenInfo.new(1, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
			{ BackgroundColor3 = Color3.fromRGB(255, 255, 255) }
		)
		tw:Play()

		
		wait(1)


		-- Tạo Sound
		local jumpscareSound = Instance.new("Sound")
		jumpscareSound.Name = "Jumpscare"
		jumpscareSound.SoundId = "rbxassetid://2916266947"
		jumpscareSound.Volume = 10
		jumpscareSound.PlaybackSpeed = 1
		jumpscareSound.Parent = workspace

		local dis = Instance.new("DistortionSoundEffect")
		dis.Level = 1
		dis.Enabled = true
		dis.Parent = jumpscareSound

		jumpscareSound:Play()

		local fullTween = TweenService:Create(
			imageLabel,
			TweenInfo.new(1.3, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
			{
				Size = UDim2.new(0, 1250, 0, 1250)
			}
		)

		fullTween:Play()

		for i = 1, 110  do
			local mode = rng:NextInteger(1, 3)

			if mode == 1 then
				background.BackgroundColor3 = Color3.new(0, 0, 0)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.2, 0.8), 0, rng:NextNumber(0.2, 0.8), 0)
			elseif mode == 2 then
				background.BackgroundColor3 = Color3.new(0, 0, 0)
				imageLabel.ImageColor3 = Color3.new(0, 0, 0)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.2, 0.8), 0, rng:NextNumber(0.2, 0.8), 0)
			else
				background.BackgroundColor3 = Color3.new(1, 1, 1)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.2, 0.8), 0, rng:NextNumber(0.2, 0.8), 0)
			end

			imageLabel.Rotation = rng:NextInteger(-10, 10)
			task.wait(0.001)
		end
		
		imageLabel:Destroy()
		
		local tw = TweenService:Create(
			background,
			TweenInfo.new(1.1, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
			{ BackgroundColor3 = Color3.fromRGB(255, 0, 0) }
		)
		tw:Play()
		
		local sound = Instance.new("Sound")
		sound.SoundId = "rbxassetid://130976109"
		sound.Volume = 10
		sound.Parent = workspace
		sound:Play()
		
		local dis = Instance.new("DistortionSoundEffect")
		dis.Level = 1
		dis.Enabled = true
		dis.Parent = sound
		wait(1.1)
		
		
		screenGui:Destroy()
		background:Destroy()
		sound:Stop()
		background2:Destroy()
